<%= form_with(model: [@group,conciert], local: true) do |form| %>
  <% if conciert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conciert.errors.count, "error") %> prohibited this conciert from being saved:</h2>

      <ul>
        <% conciert.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :people %>
    <%= form.number_field :people %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.time_select :duration %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


